modules:
  - id: root
    name: Securing a Legacy Invoice Import Service
    description: >
      Fix an eval()-based injection vulnerability in a legacy
      invoice import service by safely parsing Roman numeral
      arithmetic expressions.
    challenges:
      - id: startercode
        name: Patch an eval() Injection Vulnerability
